apiVersion: v1
kind: Secret
metadata:
  name: {{ include "spring-grpc-samples.fullname" . }}-secret-jwt
  labels:
    {{ include "spring-grpc-samples.labels" . | nindent 4 }}
type: Opaque
data:
  jwt-secret: {{ .Values.security.jwt.secret | b64enc | quote }}
